// schema.prisma

datasource db {
  provider = "mysql"
  url      = env("143.198.91.91")
}

generator client {
  provider = "prisma-client-js"
}

model Key {
  id      Int      @id @default(autoincrement())
  key     String   @unique
  expDate DateTime
}
